{
  "title": "get_post_filter_response",
  "x-stoplight": {
    "id": "msvo8jjn199v7"
  },
  "type": "object",
  "description": "Response returned for get_post_filter. It provides the active post-filter configuration along with the command execution status.",
  "properties": {
    "command": {
      "type": "string",
      "example": "get_post_filter",
      "description": "Echoes the command name so clients can map this response to the get_post_filter request."
    },
    "requestId": {
      "type": "string",
      "example": "abc123",
      "description": "Request identifier copied from the command, used for request and response correlation."
    },
    "apiVersion": {
      "type": "string",
      "enum": [
        "V1.1",
        "V1.0"
      ],
      "example": "V1.1",
      "description": "API version used to interpret this response payload."
    },
    "postFilterPayload": {
      "$ref": "../../refrence/response/postFilterResponse.yaml",
      "description": "Current post-filter configuration configured on the reader."
    },
    "response": {
      "$ref": "../../refrence/response/response.yaml",
      "description": "Execution result object containing status code and result description."
    }
  },
  "examples": [
    {
      "command": "get_post_filter",
      "requestId": "abc123",
      "apiVersion": "V1.1",
      "postFilterPayload": {
        "postFilter": [
          {
            "dataEpType": "DATA_EP1",
            "matchPattern": "FFFF",
            "matchPatternMethod": "REGEX",
            "reportOperation": "INCLUDE"
          }
        ]
      },
      "response": {
        "code": 0,
        "description": "Success"
      }
    }
  ]
}