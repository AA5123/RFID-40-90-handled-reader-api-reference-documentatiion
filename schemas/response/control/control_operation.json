{
  "title": "control_operation_response",
  "x-stoplight": {
    "id": "rp32fizym3650"
  },
  "type": "object",
  "description": "Response returned after a control_operation request is processed. It confirms the command context and reports whether the requested action succeeded.",
  "examples": [
    {
      "command": "control_operation",
      "requestId": "abc123",
      "apiVersion": "V1.1",
      "response": {
        "code": 0,
        "description": "Success"
      }
    }
  ],
  "properties": {
    "command": {
      "type": "string",
      "example": "control_operation",
      "description": "Echoes the command name so clients can map this response to the control_operation request."
    },
    "requestId": {
      "type": "string",
      "example": "abc123",
      "description": "Request identifier copied from the command, used for request and response correlation."
    },
    "apiVersion": {
      "type": "string",
      "enum": [
        "V1.0",
        "V1.1"
      ],
      "example": "V1.1",
      "description": "API version used to interpret this response payload."
    },
    "response": {
      "$ref": "../../refrence/response/response.yaml",
      "description": "Execution result object containing status code and result description."
    }
  },
  "required": [
    "command",
    "requestId",
    "apiVersion",
    "response"
  ]
}