{
  "title": "cfgAlertResponse",
  "x-stoplight": {
    "id": "bor2oqnmv18bq"
  },
  "type": "object",
  "description": "Response returned after config_alerts is processed. It confirms command context and operation result.",
  "examples": [],
  "properties": {
    "command": {
      "type": "string",
      "example": "config_alerts",
      "description": "Echoes the command name so clients can map this response to the config_alerts request."
    },
    "requestId": {
      "type": "string",
      "example": "abc123",
      "description": "Request identifier copied from the command, used for request and response correlation."
    },
    "apiVersion": {
      "type": "string",
      "example": "V1.1",
      "enum": [
        "V1.0",
        "V1.1"
      ],
      "description": "API version used to interpret this response payload."
    },
    "response": {
      "$ref": "../../refrence/response/response.yaml",
      "description": "Execution result object containing status code and result description."
    }
  }
}