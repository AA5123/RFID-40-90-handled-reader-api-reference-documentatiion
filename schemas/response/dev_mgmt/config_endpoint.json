{
  "title": "cfgAlertResponse",
  "x-stoplight": {
    "id": "kw9xt7g73rjbc"
  },
  "type": "object",
  "description": "Response returned after config_endpoint is processed. It confirms command context and operation result.",
  "examples": [
    {
      "command": "config_endpoint",
      "requestId": "abc123",
      "apiVersion": "V1.1",
      "response": {
        "code": 0,
        "description": "Success"
      }
    }
  ],
  "properties": {
    "command": {
      "type": "string",
      "example": "config_endpoint",
      "description": "Echoes the command name so clients can map this response to the config_endpoint request."
    },
    "requestId": {
      "type": "string",
      "example": "abc123",
      "description": "Request identifier copied from the command, used for request and response correlation."
    },
    "apiVersion": {
      "type": "string",
      "example": "V1.1",
      "enum": [
        "V1.0",
        "V1.1"
      ],
      "description": "API version used to interpret this response payload."
    },
    "response": {
      "$ref": "../../refrence/response/response.yaml",
      "description": "Execution result object containing status code and result description."
    }
  }
}