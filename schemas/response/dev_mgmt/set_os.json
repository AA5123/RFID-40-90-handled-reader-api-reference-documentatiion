{
  "title": "setOSResponse",
  "x-stoplight": {
    "id": "esbrwfwavpx3o"
  },
  "type": "object",
  "description": "Response returned after set_os is processed. It confirms command context and operation result.",
  "examples": [
     {
       "command": "set_os",
       "requestId": "18996",
       "apiVersion": "V1.1",
       "response": {
           "code": 1,
            "description": "Command payload is accepted"
        }
     }
  ],
  "properties": {
    "command": {
      "type": "string",
      "example": "set_os",
      "description": "Echoes the command name so clients can map this response to the set_os request."
    },
    "requestId": {
      "type": "string",
      "example": "abc123",
      "description": "Request identifier copied from the command, used for request and response correlation."
    },
    "apiVersion": {
      "type": "string",
      "enum": [
        "V1.0",
        "V1.1"
      ],
      "example": "V1.1",
      "description": "API version used to interpret this response payload."
    },
    "response": {
      "$ref": "../../refrence/response/response.yaml",
      "description": "Execution result object containing status code and result description."
    }
  },
  "required": [
    "command",
    "requestId",
    "apiVersion"
  ]
}