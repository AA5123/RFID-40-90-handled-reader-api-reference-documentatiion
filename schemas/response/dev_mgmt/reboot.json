{
  "title": "setWifiResponse",
  "x-stoplight": {
    "id": "vdmaqk96eai0f"
  },
  "type": "object",
  "description": "Response returned after reboot is processed. It confirms command context and operation result.",
  "examples": [
     {
       "command": "reboot",
       "requestId": "18996",
       "apiVersion": "V1.1",
       "response": {
           "code": 1,
            "description": "Command payload is accepted"
        }
     }
  ],
  "properties": {
    "command": {
      "type": "string",
      "example": "reboot",
      "description": "Echoes the command name so clients can map this response to the reboot request."
    },
    "requestId": {
      "type": "string",
      "example": "123abcd",
      "description": "Request identifier copied from the command, used for request and response correlation."
    },
    "apiVersion": {
      "type": "string",
      "enum": [
        "V1.0",
        "V1.1"
      ],
      "example": "V1.1",
      "description": "API version used to interpret this response payload."
    },
    "response": {
      "$ref": "../../refrence/response/response.yaml",
      "description": "Execution result object containing status code and result description."
    }
  },
  "required": [
    "command",
    "requestId",
    "apiVersion",
    "response"
  ]
}