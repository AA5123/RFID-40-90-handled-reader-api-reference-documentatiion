{
  "title": "get_endpoint_config",
  "x-stoplight": {
    "id": "n4h9ertlb1qn8"
  },
  "type": "object",
  "description": "Retrieves endpoint configuration. If endpointDetails.endpointName is not provided, the response returns all active endpoint configurations and saved endpoint names. If endpointName is provided, only that endpoint configuration is returned.",
  "x-internal": false,
  "properties": {
    "command": {
      "type": "string",
      "default": "get_endpoint_config",
      "description": "Command name for requesting endpoint configuration details."
    },
    "requestId": {
      "type": "string",
      "example": "123",
      "description": "Unique request identifier used to correlate this command with its response."
    },
    "endpointDetails": {
      "$ref": "../../refrence/payload/epName.yaml",
      "description": "Optional object used to request a specific endpoint configuration by endpoint name."
    }
  },
  "required": [
    "command",
    "requestId"
  ],
  "examples": [
    {
      "command": "get_endpoint_config",
      "requestId": "abc123"
    },
    {
      "command": "get_endpoint_config",
      "requestId": "def456",
      "endpointDetails": {
        "endpointName": "ctrl"
      }
    }
  ]
}