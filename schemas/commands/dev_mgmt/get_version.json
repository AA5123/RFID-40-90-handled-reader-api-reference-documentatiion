{
  "title": "get_version",
  "x-stoplight": {
    "id": "6t1zk44tshzm1"
  },
  "description": "Retrieves reader identity and software version details, including model, serial number, SKU, and firmware information.",
  "type": "object",
  "examples": [],
  "properties": {
    "command": {
      "type": "string",
      "example": "get_version",
      "description": "Command name for requesting reader identity and version details."
    },
    "requestId": {
      "type": "string",
      "example": "abc123",
      "description": "Unique request identifier used to correlate this command with its response."
    }
  },
  "required": [
    "command",
    "requestId"
  ]
}