{
  "title": "get_status",
  "x-stoplight": {
    "id": "2a0e5dee1bcgq"
  },
  "description": "Retrieves current reader runtime status, such as power source, radio state, connectivity, and battery information.",
  "type": "object",
  "properties": {
    "command": {
      "type": "string",
      "example": "get_status",
      "description": "Command name for requesting current device status information."
    },
    "requestId": {
      "type": "string",
      "example": "abcd123",
      "description": "Unique request identifier used to correlate this command with its response."
    }
  },
  "required": [
    "command",
    "requestId"
  ]
}