{
  "title": "get_config",
  "x-stoplight": {
    "id": "tzc9hszry476y"
  },
  "description": "Retrieves consolidated device configuration, including version details, status, region settings, network configuration, endpoint configuration, installed certificates, and event or alert settings.",
  "type": "object",
  "examples":[],
  "properties": {
    "command": {
      "type": "string",
      "example": "get_config",
      "description": "Command name for requesting complete device configuration."
    },
    "requestId": {
      "type": "string",
      "example": "abc123",
      "description": "Unique request identifier used to correlate this command with its response."
    }
  }
}