{
  "title": "get_wifi",
  "x-stoplight": {
    "id": "67cwfou98hctb"
  },
  "description": "Retrieves saved Wi-Fi profiles and current Wi-Fi interface details from the reader.",
  "type": "object",
  "properties": {
    "command": {
      "type": "string",
      "example": "get_wifi",
      "description": "Command name for requesting Wi-Fi configuration details."
    },
    "requestId": {
      "type": "string",
      "example": "abc123",
      "description": "Unique request identifier used to correlate this command with its response."
    }
  },
  "required": [
    "command",
    "requestId"
  ]
}