{
  "title": "get_current_region",
  "x-stoplight": {
    "id": "v4uqym3mk9bt0"
  },
  "description": "Retrieves the current regulatory region configuration applied on the reader.",
  "type": "object",
  "properties": {
    "command": {
      "type": "string",
      "example": "get_current_region",
      "description": "Command name for requesting current region configuration."
    },
    "requestId": {
      "type": "string",
      "example": "abc123",
      "description": "Unique request identifier used to correlate this command with its response."
    }
  }
}