{
  "title": "get_installed_certificate",
  "x-stoplight": {
    "id": "9bjcurdina4tr"
  },
  "description": "Retrieves certificate inventory currently installed on the reader for supported features.",
  "type": "object",
  "examples": [],
  "properties": {
    "command": {
      "type": "string",
      "example": "get_installed_certificates",
      "description": "Command name for requesting installed certificate details."
    },
    "requestId": {
      "type": "string",
      "example": "abc123",
      "description": "Unique request identifier used to correlate this command with its response."
    }
  }
}