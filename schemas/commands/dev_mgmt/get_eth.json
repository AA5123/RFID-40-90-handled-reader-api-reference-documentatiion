{
  "title": "get_eth",
  "x-stoplight": {
    "id": "np1ehj39n4xp4"
  },
  "description": "Retrieves Ethernet interface configuration and link status. Security features, static IPv4 configuration, and IPv6 are not available in the current API version.",
  "type": "object",
  "examples": [],
  "properties": {
    "command": {
      "type": "string",
      "example": "get_eth",
      "description": "Command name for requesting Ethernet configuration details."
    },
    "requestId": {
      "type": "string",
      "example": "abc123",
      "description": "Unique request identifier used to correlate this command with its response."
    }
  },
  "required": [
    "command",
    "requestId"
  ]
}